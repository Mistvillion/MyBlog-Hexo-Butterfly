---
title: 写博客标准流程
categories:
  - Prepare
  - test_article
comments: true
description: 写博客标准流程，使用git和GitHub来管理仓库
highlight_shrink: true
cover: /img/posts/GithubRepositoryManage.png
abbrlink: 7a6a6b5a
date: 2025-11-17 00:00:00
updated: 2025-11-18 00:00:00
---

## 📘 写博客标准流程（Hexo + Butterfly + abbrlink + GitHub 双仓库）

本文由 ChatGPT 5.1 生成

> **作者：silentnight-sparklerain**
> **博客框架：Hexo + Butterfly**
> **源码仓库：MyBlog-Hexo-Butterfly**
> **部署仓库：silentnight-sparklerain.github.io**

---

## ⭐ 1. 博客工程目录（源码目录）

Hexo 博客源码目录包含：

* `_config.yml`
* `_config.butterfly.yml`
* `source/`
* `themes/`
* `scaffolds/`
* `package.json`
* `node_modules/`
* `.gitignore`

👉 **所有写文章 / 改配置 / 改主题 都在这个目录完成。**

---

## ⭐ 2. GitHub 两个仓库的用途（必须分清）

| 仓库名                                   | 用途        | 内容                 | 是否手动操作？ |
| ------------------------------------- | --------- | ------------------ | ------- |
| **MyBlog-Hexo-Butterfly**             | Hexo 源码仓库 | Markdown 原稿、主题、配置  | ✔ 经常操作  |
| **silentnight-sparklerain.github.io** | 部署仓库（自动）  | 生成后的 HTML/CSS 静态网站 | ❌ 不手动改  |

👉 平常只碰 **MyBlog-Hexo-Butterfly**。
👉 网站更新由 Hexo 自动推送到 `silentnight-sparklerain.github.io`。

---

## ⭐ 3. 新建文章（Hexo 自动生成 abbrlink）

在源码目录执行：

```bash
hexo new post "文章标题"
```

Hexo 会自动生成：

```
source/_posts/文章标题.md
```

并自动写入一个固定的 `abbrlink`，保证 **评论永不丢**。

---

## ⭐ 4. 写文章 / 改文章

直接编辑 `source/_posts/*.md` 文件即可。

可以随便改：

* title
* 内容
* 标签
* 分类

**不会影响评论，因为 URL 由 abbrlink 决定，不会变。**

---

## ⭐ 5. 本地预览（强烈建议每次发布前预览）

```bash
hexo s
```

然后打开：

```
http://localhost:4000
```

确认页面效果是否正常。

按 `Ctrl + C` 退出预览。

---

## ⭐ 6. 生成 + 部署（发布到 GitHub Pages）

```bash
hexo clean
hexo g
hexo d
```

Hexo 会自动：

* 在 `public/` 生成静态网站
* 推送到 GitHub 的 `silentnight-sparklerain.github.io` 仓库
* 完成 GitHub Pages 发布

👉 **你不需要手动修改部署仓库。**

---

## ⭐ 7. 提交源码到 MyBlog-Hexo-Butterfly（非常重要）

写完博客后，把源码同步到你的源码仓库：

```bash
git add .
git commit -m "新增文章：xxx / 修改配置 / 更新主题"
git push
```

这样可以：

* 防止换电脑丢文件
* 所有配置、主题、文章都记录在 Git
* 以后任何电脑都能恢复完整环境

---

## ⭐ 8. 换电脑写博客 / 多台电脑同步

在新电脑上：

```bash
git clone https://github.com/silentnight-sparklerain/MyBlog-Hexo-Butterfly.git
cd MyBlog-Hexo-Butterfly
npm install
```

写文章前，务必：

```bash
git pull
```

确保你的代码是最新的。

之后：

```
hexo new / hexo s / hexo g / hexo d / git push
```

全部流程完全一样。

---

## ⭐ 9. 不要做的事（防止搞乱）

❌ 不要手动修改
`silentnight-sparklerain.github.io`
这个仓库只给 Hexo 自动用。

❌ 不要随便删除 Markdown 原稿（source/_posts）。

❌ 不要删除 `abbrlink` 字段（否则会导致评论重新计数）。

---

## ⭐ 10. 常用命令速查表

| 功能               | 命令                                          |
| ---------------- | ------------------------------------------- |
| 新建文章             | `hexo new post "标题"`                        |
| 本地预览             | `hexo s`                                    |
| 清理缓存             | `hexo clean`                                |
| 生成静态文件           | `hexo g`                                    |
| 发布到 GitHub Pages | `hexo d`                                    |
| 提交源码             | `git add . && git commit -m "" && git push` |
| 同步源码             | `git pull`                                  |

---

## ⭐ 11. 评论永不丢的保证（你已全部设置完成）

你的博客已采用：

* `hexo-abbrlink` → 固定文章唯一 ID
* `permalink: posts/:abbrlink/` → URL 永远不变
* `giscus.mapping = pathname` → 评论根据路径识别

无论你：

* 改标题
* 改内容
* 改 front-matter
* 整站重建

👉 **评论永远不会丢。**

---

## ✔ 结束语

你现在的博客已经具有：

* 稳定链接
* 稳定评论系统
* 可多电脑同步
* 完整 Git 版本管理
* 自动部署 GitHub Pages

这是 Hexo + Butterfly 的最佳实践方案。

